var startTime = clock();
var a = "global a";
var b = "global b";
var c = "global c";

{
  var a = "outer a";
  var b = "outer b";

  {
    var a = "inner a";

    print a;
    print b;
    print c;
  }

  print a;
  print b;
  print c;
}

print a;
print b;
print c;

if (true) {
  print "true";
}

if (false) {
  print "false";
}

print nil or "hello";
print nil and "hello";

var count = 0;

while (count < 10) {
  print count;
  count = count + 1;
}

// Fibonacci
var a = 0;
var temp;

for (var i = 1; a < 10000; i = temp + i) {
  print a;
  temp = a;
  a = i;
}

// Native function call
print "Script run in (ms)";
print clock() - startTime;
