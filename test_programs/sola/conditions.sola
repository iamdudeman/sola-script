if (true) {
  print("true");
}

if (false) {

} else {
  print("false");
}

val trueTernary = true ? "true" : "false";

print(trueTernary);

val falseTernary = false ? "true" : "false";

print(falseTernary);

// nested ternary requires parens
val nestedTernary = true ? (false ? "wrong" : "correct") : "wrong";

print(nestedTernary);
